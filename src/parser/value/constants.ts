/**
 * List of string values that are interpreted as `true`.
 *
 * All values are expected to be lowercase and should be compared
 * against normalized (trimmed and lowercased) input.
 */
export const TRUE_VALUES = ['true', '1', 'yes', 'on'];

/**
 * List of string values that are interpreted as `false`.
 *
 * All values are expected to be lowercase and should be compared
 * against normalized (trimmed and lowercased) input.
 */
export const FALSE_VALUES = ['false', '0', 'no', 'off'];
